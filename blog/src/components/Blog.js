import React from 'react';

const Blog = () => {
  return (
    <div>
      <h1>My First BLog</h1>
      <p>If the "siteMetadata" field is indeed available on the "Query" type, then the error may be due to incorrect syntax in your query. Make sure that you are using the correct field names and query structure according to the GraphQL specification.</p>
    </div>
  );
}

export default Blog;